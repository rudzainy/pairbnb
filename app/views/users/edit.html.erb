<%= form_for @user do |f| %>
	<div class="form-container">
		<div class="form-header">
			Edit Profile
		</div>
	
		<div class="form-body">
			<%= render 'shared/error_messages', object: f.object %>
			<table>
				<tr>
					<td class="form-label"><%= f.label :name %></td>
					<td class="form-box"><%= f.text_field :name, class: 'form-control', placeholder: :name, required: true %></td>
				</tr>
				<tr>
					<td class="form-label"><%= f.label :email %></td>
					<td class="form-box"><%= f.email_field :email, class: 'form-control', placeholder: 'Email Address', required: true %></td>
				</tr>
			</table>
		</div>
	</div>
	<%= f.submit 'Save', class: 'btn btn-block btn-large btn-pink center-layout'%>
<% end %>
<br><br>
<%= form_for @user, method: 'delete' do |f| %>
	<div class="form-container">
		<div class="form-header">
			Delete account
		</div>
	
		<div class="form-body">
			<%= f.check_box :name, class: 'form-check-box', required: true %> Confirm deletion
		</div>
	</div>
	<%= f.submit 'Delete account', class: 'btn btn-block btn-large btn-pink center-layout'%>
<% end %>